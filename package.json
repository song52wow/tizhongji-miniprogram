{
  "name": "tizhongji-miniprogram",
  "version": "1.0.0",
  "description": "体重管理微信小程序",
  "scripts": {
    "build:ts": "tsc",
    "dev": "npm run build && mp dev",
    "build": "npm run build:ts && mp build",
    "wechat": "npm run build"
  },
  "devDependencies": {
    "miniprogram-build": "^7.0.3",
    "typescript": "^6.0.3"
  }
}
