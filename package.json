{
  "name": "tizhongji-miniprogram",
  "version": "1.0.0",
  "description": "体重管理微信小程序",
  "scripts": {
    "build:ts": "tsc",
    "dev": "npm run build:ts && miniprogram build --watch",
    "build": "npm run build:ts && miniprogram build"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
